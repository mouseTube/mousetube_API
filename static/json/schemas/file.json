{
    "title": "File",
    "description": "Metadata for a single recording file.",
    "type": "object",
    "properties": {
      "name": {
        "description": "Name of the file.",
        "type": "string"
      },
      "date": {
        "description": "Date when the recording was made (ISO format).",
        "type": "string",
        "format": "date"
      },
      "sampling_rate": {
        "description": "Sampling rate of the recording in Hz (e.g., 300000).",
        "type": "number",
        "minimum": 1
      },
      "bit_depth": {
        "description": "Bit depth of the recording (e.g., 16, 24).",
        "type": "number",
        "enum": [8, 16, 24, 32]
      },
      "notes": {
        "description": "Optional notes about this specific file.",
        "type": "string"
      },
      "size": {
        "description": "Size of the file in bytes.",
        "type": "integer",
        "minimum": 0
      },
      "doi": {
        "description": "Digital Object Identifier (DOI) for the file, if applicable.",
        "type": "string",
        "format": "uri"
      },
      "number": {
        "description": "Number of vocalization in the recording, if applicable.",
        "type": "integer",
        "minimum": 1
      }
    },
    "required": ["sampling_rate", "bit_depth", "date", "name"]
  }