{
	"type": "object",
	"properties": {
	  "name": {
		"type": "string",
		"title": "Session Name",
		"description": "Name of the recording session (must be unique per user)."
	  },
	  "description": {
		"type": "string",
		"title": "Description",
		"description": "Optional description of the recording session."
	  },
	  "studies": {
		"type": "array",
		"title": "Associated Studies",
		"description": "List of studies associated with this recording session.",
		"items": {
		  "$ref": "study.json"
		}
	  },
	  "date": {
		"type": "string",
		"format": "date-time",
		"title": "Recording Date",
		"description": "Date and time when the recording session was conducted."
	  },
	  "duration": {
		"type": "integer",
		"title": "Duration (seconds)",
		"description": "Duration of the recording session in seconds."
	  },
	  "laboratory": {
		"$ref": "laboratory.json",
		"title": "Laboratory",
		"description": "Laboratory where the session was conducted."
	  },
	  "equipment": {
		"type": "object",
		"title": "Equipment Used",
		"description": "Equipment used during the recording session.",
		"properties": {
		  "soundcards": {
			"type": "array",
			"title": "Soundcards",
			"description": "Soundcards used during acquisition (required if used).",
			"items": {
			  "$ref": "hardware.json"
			}
		  },
		  "microphones": {
			"type": "array",
			"title": "Microphones",
			"description": "Microphones used during acquisition (required if used).",
			"items": {
			  "$ref": "hardware.json"
			}
		  },
		  "amplifiers": {
			"type": "array",
			"title": "Amplifiers",
			"description": "Amplifiers used during acquisition (optional).",
			"items": {
			  "$ref": "hardware.json"
			}
		  },
		  "speakers": {
			"type": "array",
			"title": "Speakers",
			"description": "Speakers used during acquisition (optional).",
			"items": {
			  "$ref": "hardware.json"
			}
		  },
		  "acquisition_software": {
			"type": "array",
			"title": "Acquisition Software",
			"description": "Software used for recording audio (required if used).",
			"items": {
			  "type": "object",
			  "properties": {
				"software": {
				  "$ref": "software.json"
				},
				"version": {
				  "type": "string",
				  "pattern": "^(\\d+\\.)?(\\d+\\.)?(\\*|\\d+)$",
				  "title": "Version",
				  "description": "Version of the software used."
				},
				"release_date": {
				  "type": "string",
				  "format": "date",
				  "title": "Release Date",
				  "description": "Release date of the software version."
				}
			  },
			  "required": ["software"],
			  "additionalProperties": false
			}
		  },
		  "channels": {
			"type": "string",
			"enum": ["mono", "stereo", "more than 2"],
			"title": "Channels",
			"description": "Number of microphones used during recording."
		  },
		  "sound_isolation": {
			"type": "string",
			"enum": ["soundproof room", "soundproof cage", "no specific sound isolation"],
			"title": "Sound Isolation",
			"description": "Sound isolation used during recording."
		  }
		},
		"required": ["soundcards", "microphones", "acquisition_software"]
	  },
	  "context": {
		"type": "object",
		"title": "Context",
		"description": "Environmental context during the recording session.",
		"properties": {
		  "temperature": {
			"type": "object",
			"title": "Temperature",
			"properties": {
			  "value": {
				"type": "integer",
				"title": "Temperature Value",
				"description": "Value of the temperature during recording."
			  },
			  "unit": {
				"type": "string",
				"enum": ["°C", "°F"],
				"title": "Temperature Unit",
				"description": "Unit of the temperature measurement."
			  }
			},
			"required": ["value", "unit"]
		  },
		  "brightness": {
			"type": "integer",
			"title": "Brightness (Lux)",
			"description": "Brightness of the recording environment in Lux."
		  }
		}
	  }
	},
	"required": ["name", "equipment"]
  }
  